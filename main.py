from evasions import encoding
import pyfiglet

ascii_banner = pyfiglet.figlet_format("Path Trasverser")
print(ascii_banner)
def main():
    encoding.url_encoding(link, payload)
    encoding.unicode_encoding(link, payload)
if __name__ in "__main__":
    link =  input("input the website link:")
    payload = input("Enter the file path you are looking for:")
    main = main()